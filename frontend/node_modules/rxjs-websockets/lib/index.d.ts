import { Observable } from 'rxjs/Observable';
export interface Connection {
    connectionStatus: Observable<any>;
    messages: Observable<any>;
}
export interface IWebSocket {
    close(): any;
    send(string: any): any;
    onopen: Function;
    onclose: Function;
    onmessage: Function;
    onerror: Function;
}
export declare type WebSocketFactory = (url: String) => IWebSocket;
export default function connect(url: string, input: Observable<any>, websocketFactory?: WebSocketFactory, jsonParse?: boolean): Connection;
